package a_primeros_pasos;

public class WritingMain {
	
	// Main method is the gateway between the startup of a java process and the beginning of the programmers code
	/*
	 * to compile Java code the file must have the extension .java
	 * the name of the file must match the name of the class
	 * the result is a bytecode file with .class
	 * bytecode  have instructions that JVM knows how execute
	 */
	
	//public is an access modifier, we,ll see others ahead
	//static binds a method to its class so it can be called by just the class name
	// if the main method haven,t the static keyword it would throw an exception
	/* 
	 * void represents the returns type
	 * it,s a good practice to use void method that change a object,s state
	 * the main method changes the program state from start to finish 
	 */
	/*
	 * the main method parameters is an arry of java.lang.String objects
	 * you can write  String [] args , String args[] or String... args;
	 * you can use args or other name you like
	 */

	/*
	 * To review, you need to have a JDK to compile because it includes a compiler. You do not 
	 * need to have a JDK to run the code—a JRE is enough.
	 * Java class files run on the JVM and
	 * therefore run on any machine with Java
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
